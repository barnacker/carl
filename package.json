{
  "name": "carl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CARL Stage 0 executable spine prototype.",
  "bin": {
    "carltest": "./bin/carltest.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/**/*.test.js tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
